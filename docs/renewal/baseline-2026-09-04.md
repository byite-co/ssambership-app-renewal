# 기준선 — 2026-09-04 (A-1 부트스트랩)

리뉴얼 시작 시점에 **저장소에 무엇이 있는가**를 실측으로 기록한다. 기준 커밋: `master` `635ae73` (원본 `ssambership-app` 미러).
추측으로 "통과"라고 쓴 항목은 없다 — 돌리지 못한 것은 못 돌렸다고 적었다.

---

## 1. 빌드가 되는가 (§2-1)

### 환경

| 항목 | 값 |
|---|---|
| 실행 환경 | Linux x86_64 컨테이너(클라우드 세션), 15 GB RAM |
| Flutter SDK | **처음엔 없었다.** CI 고정 버전과 같은 **3.44.6 stable** tarball 을 내려받아 세션 임시 디렉터리에 설치해 실제로 돌렸다 (Dart 3.12.2, engine `d3a3293`) |
| Android SDK | **없다.** `dl.google.com` 이 세션 프록시에서 403 으로 차단돼 cmdline-tools 를 받을 수 없었다 |
| `.env` | `.env.example` 을 복사한 자리표시 값(pubspec 에셋 요건 — CI 와 동일) |

### 결과

| 명령 | 결과 | 비고 |
|---|---|---|
| `flutter pub get` | **성공** | "63 packages have newer versions incompatible with dependency constraints" — 정보성. `pubspec.lock` 은 커밋 대상(재현 가능) |
| `flutter analyze` | **error 0 · warning 0 · info 79** (32초) | exit code 는 1 — Flutter 3.44 는 info 만 있어도 1 을 돌려준다(CI 가 출력 파싱으로 판정하는 이유). info 79건은 `prefer_initializing_formals`·`unnecessary_import`·deprecated API 류 |
| `flutter test` | **1,510 통과 · 0 실패** (약 3분) | 골든 추가 전 기준. 골든 7건 추가 후 최종 수치는 `bootstrap-report-2026-09-04.md` §회귀 기준선 |
| `flutter build apk --debug` | **실행 못 함** | Android SDK 없음 + 네트워크 차단. 대체: CI `flutter-ci.yml` 의 `appbundle` 단계가 파이프라인 생존을 확인한다(게이트 아님). 원본 저장소의 마지막 CI(`ci-logs` 브랜치: run 33833624237, commit `a0a1ff26` — 이 저장소 이력에는 없는 커밋)는 analyze·test·appbundle 전부 success 였다. **이 저장소에서의 CI 실행 결과는 부트스트랩 보고서 참조** |

실패 원인 중 이번에 고친 것은 없다(방침).

---

## 2. 테스트 현황 (§2-2)

### 2-1. 규모와 분류

- `test/` 아래 Dart 파일 **175개** = `*_test.dart` **173개** + 공용 fake 2개(`test/community/fakes.dart`, `test/version_gate/version_gate_fakes.dart`).
  지시서의 "69개"는 다른 시점(아마 `chore/release-config` 브랜치)의 수치다 — `master` 기준은 173이다.
- 선언 기준: `testWidgets(` **565** · `test(` **905** (실행 시 group/반복으로 1,510건이 된다).
- **단위(unit)**: 레포지토리·모델·파서·포맷터·정책(entitlement·version_gate·deeplink·push·web_bridge·labels·ink·data·core·auth·tool).
- **위젯(widget)**: 화면·섹션·공용 위젯을 `MaterialApp` 으로 감싸 fake 주입 렌더(screens·widgets·community·individual_question·mypage·mentors·notifications·refresh·scan_annotation·shared·design·core_scan·core_ink).
- **통합(integration)**: `integration_test/e2e_staging_test.dart` 1개(+`test_driver/integration_test.dart`). `flutter drive` 로 실서버(staging)에 붙는다 — **`flutter test` 스위트에 포함되지 않으며 이번에 돌리지 않았다**(DB 접속 금지).
- **골든**: **없었다.** (`matchesGoldenFile` 사용 0건.) → A-1 에서 `test/goldens/` 신설.
- mock 패키지 없음 — 전부 손코딩 fake(`extends`/`implements`).

| 디렉터리 | 파일 | testWidgets | test | 성격 |
|---|---|---|---|---|
| test/community | 26 | 92 | 186 | 위젯+단위(게시판·숏폼·댓글·차단·게이트웨이 봉투) |
| test/individual_question | 20 | 100 | 130 | 위젯+단위(IQ 목록·상세·첨부·첨삭·실시간) |
| test/data | 16 | 3 | 103 | 단위(모델·레포·매핑·커서) |
| test/screens | 15 | 71 | 7 | 위젯(질문방 화면군·홈셸·진입가드) |
| test/widgets | 16 | 54 | 2 | 위젯(말풍선·입력바·카드·배지) |
| test/mypage | 14 | 86 | 51 | 위젯+단위 |
| test/mentors | 9 | 29 | 52 | 위젯+단위 |
| test/version_gate | 7 | 13 | 34 | 단위+위젯(게이트 셸) |
| test/contracts | 6 | 5 | 78 | **계약**(아래) |
| test/notifications | 6 | 17 | 36 | 위젯+단위 |
| test/scan_annotation | 6 | 9 | 6 | 위젯+단위 |
| test/design | 4 | 11 | 8 | 위젯(테마·역할색) |
| test/refresh | 4 | 38 | 3 | 위젯(재조회 버스) |
| test/web_bridge | 4 | 3 | 28 | 단위 |
| test/core_scan | 3 | 15 | 15 | 위젯+단위 |
| test/ink | 3 | 0 | 18 | 단위 |
| test/auth | 2 | 0 | 34 | 단위(계정상태 fail-closed·접근상태) |
| test/core | 2 | 0 | 17 | 단위 |
| test/deeplink | 2 | 0 | 29 | 단위 |
| test/entitlement | 2 | 0 | 25 | 단위 |
| test/push | 2 | 0 | 14 | 단위(firebase 0 유지) |
| test/shared | 2 | 12 | 6 | 위젯 |
| test/app | 1 | 0 | 2 | 단위(pubspec 버전 잠금) |
| test/core_ink | 1 | 7 | 0 | 위젯 |
| test/labels | 1 | 0 | 7 | 단위 |
| test/tool | 1 | 0 | 14 | 단위(release env 검증 도구) |

### 2-2. 계약 테스트 — 실재하며, 무엇을 잠그는가

CC 앱 조사(9/3)가 말한 "매니페스트 잠금 테스트·CI" 는 **실재한다.** `test/contracts/` 6개 + 파일 잠금 성격의 단위 테스트 2개.

**① 아웃바운드 API 매니페스트** — `test/contracts/outbound_api_manifest_test.dart`
`lib/` 소스를 정규식으로 스캔(주석 제거)해 앱이 부르는 서버 표면의 **전체 집합**을 고정한다.

| 잠그는 것 | 위치 | 내용 |
|---|---|---|
| RPC 리터럴 집합 | `:15-59` `kExpectedRpcNames` | 34개(계정 탈퇴 5 · 프로필 1 · 질문방 `qna_*` 9 · 멘토 1 · 개별질문 7 · 커뮤니티 5 · 알림 3 · 버전 게이트 1). 추가/삭제 모두 실패 |
| RPC 식별자 호출 | `:63-67`, 검사 `:199-214` | `fn`·`kBoardPostCreateFunction`·`kBoardPostUpdateFunction` 만 허용 + 상수 값(`community_post_create/update`) 대조 |
| 스키마 | `:69-74`, 검사 `:216-235` | `{api_app_v1, api_web_v1}` + 식별자 `kBoardPostCreateSchema`·`schema` |
| 테이블/뷰 리터럴 | `:77-110` `kExpectedTables`, 검사 `:237-255` | 25개(users·subscriptions·my_wallet_v1·question_threads·mentor_directory_v1·community_posts_v1 …). Storage `.from()` 리터럴은 **0건** 강제 |
| 테이블 식별자 | `:113-117`, 검사 `:257-275` | `_table`·`_reportsTable`·`table` |
| 버킷 상수·값 | `:120-137`, 검사 `:257-292` | 6 버킷(`question-room-attachments`·`individual-question-attachments`·`scan-annotations`·`connection-note-ink`·`shortform-videos`·`community-post-images`) — 이름 뒤에 숨는 버킷 차단 |
| 금지 표면 | `:140-149`, 검사 `:294-303` | REVOKE 된 8개(`mentor_directory_list_v2`·`record_cash_topup`·`subscription_checkout_confirm` …) 0건 |
| `users` SELECT 전용 | `:305-319` | 같은 체인에 `.update(`/`.insert(` 금지 |
| `community_posts` 베이스 테이블 접근 0 | `:321-328` | 뷰/RPC 단일 경로 |
| `firebase` 문자열 0 | `:330-336` | App-F0(OS 푸시 미도입) 유지 |
| 가드 자기검증 | `:338-386` | 정규식이 실제 위반에 반응하는지 |

리뉴얼 시사점: A-4(기능 개방)에서 서버 표면이 늘면 이 파일을 **같은 PR 에서** 갱신해야 한다. "의도된 변경만 통과" 가 설계 의도이므로 유지한다.

**② Android 서명 워크플로 계약** — `test/contracts/android_signed_workflow_contract_test.dart`
`.github/workflows/android-signed-release-candidate.yml` 을 YAML 구조 파싱: `workflow_dispatch` 단독, 사람 확인 입력 2종, 최소 권한, Environment `android-release-candidate`, `SOURCE_SHA` 40자 고정(자유 입력 금지), 기대 버전 `1.0.0+19`·패키지·URL·**테스트 수 1508**, 외부 action SHA 핀, bundletool 버전+checksum, 금지 문자열(secret 유출 경로), Play 업로드 step 없음, upload-artifact 1개·retention 3일, cleanup 마지막 always(), 문서 정본(`docs/ANDROID_BUILD.md` `:348`).

**③ iOS 출시 설정 계약** — `test/contracts/ios_release_config_contract_test.dart`
Info.plist(표시명·`ITSAppUsesNonExemptEncryption`·`LSApplicationQueriesSchemes` allowlist·ATS·권한 문구), `PrivacyInfo.xcprivacy`(추적 없음·수집 데이터 인벤토리), `project.pbxproj`(번들 ID `com.ssambership.app` 3구성+RunnerTests 3구성 `:323`, 배포 타깃 13.0, Automatic 서명), Podfile, **pubspec 버전 `1.0.0+19` 고정 `:360-365`**, `.env`/keystore 비추적 `:366`, 정본 런북 `docs/IOS_RELEASE_RUNBOOK.md` 존재 `:19,:388` · `docs/IOS_BUILD_PLAN.md` 부재 `:406` · `docs/IOS_BUILD.md` 스텁 `:409`.

**④ 개별질문 등록 경계** — `test/contracts/iq_create_boundary_test.dart`
production 코드에서 `IqCreateScreen` import/생성 0건(자기 파일 제외), 라우트·딥링크에 네이티브 등록 경로 없음, 두 CTA 가 웹 브릿지 URL(`/individual-questions/new`, `/mentors/{id}/individual-question/new`)만 호출. **A-4 에서 이 경계를 여는 순간 의도적으로 갱신해야 하는 테스트.**

**⑤ S3-E 문서 정본** — `test/contracts/s3e_doc_contract_test.dart` → `docs/S3E_QUESTION_ROOM_SAFETY_CONTRACT.md` 의 신고 어휘·서버 차단 PASS 플래그 문구 고정.

**⑥ Xcode Cloud 부트스트랩** — `test/contracts/xcode_cloud_bootstrap_contract_test.dart` → `ios/ci_scripts/ci_post_clone.sh` 존재·실행권한(100755)·`set -eu`·Flutter **3.44.6** 핀·fail-closed `.env`·ephemeral 산출물 비추적·pbxproj SwiftPM 배선.

**파일 잠금 성격의 단위 테스트**
- `test/app/build_version_test.dart:37-62` — `pubspec.yaml` `version: 1.0.0+19` 와 `AppConstants.appVersion == '1.0.0'` 고정. **버전을 올릴 때 ②·③과 함께 3곳을 갱신해야 한다.**
- `test/push/firebase_free_test.dart` — pubspec·lib 에 firebase 0.

**문서 경로 의존**: ②③⑤가 `docs/ANDROID_BUILD.md`·`docs/IOS_BUILD.md`·`docs/IOS_RELEASE_RUNBOOK.md`·`docs/S3E_QUESTION_ROOM_SAFETY_CONTRACT.md` 를 경로로 읽는다. 그래서 이 4개는 `docs/legacy/` 로 옮기지 않았다.

### 2-3. 골든 테스트

**없었다.** A-1 에서 `test/goldens/` 를 신설했다(부트스트랩 보고서).

---

## 3. CI (§2-3)

`.github/workflows/` 에 2개가 있다 — `chore/release-config` 브랜치에는 없었지만 **`master` 에는 있다.**

### `flutter-ci.yml`

| 항목 | 내용 |
|---|---|
| 트리거 | `push`(master) · `pull_request`(master) · `workflow_dispatch` |
| 검사 | `cp .env.example .env` → `flutter pub get` → **`flutter analyze`**(출력 파싱: error/warning 1건이면 실패, info 비차단) → **`flutter test`**(전체) → `flutter build appbundle --release`(`allowInsecureSigning=true` 로 debug 서명 폴백 — 파이프라인 생존 확인, **게이트 아님**) → 로그를 `ci-logs` 브랜치로 force-push → 게이트 판정(analyze+test) |
| 툴체인 | ubuntu-latest · temurin JDK 17 · Flutter **3.44.6** stable |
| 시크릿 | **없음** — `GITHUB_TOKEN` 만(ci-logs 발행, `permissions: contents: write`) |
| 원본 저장소 참조 | 없음(`${GITHUB_REPOSITORY}` 동적). **새 저장소에서 그대로 동작 가능** |
| 실행 이력 | 새 저장소 Actions 실행 **0건**(API 확인). `ci-logs` 브랜치 내용은 원본 저장소 실행분 |

A-1 변경: 골든 실패 diff(`test/**/failures`) 업로드 + 기준 골든 PNG 업로드 step 추가, 주석 현행화. 게이트 로직은 그대로.

### `android-signed-release-candidate.yml`

| 항목 | 내용 |
|---|---|
| 트리거 | `workflow_dispatch` **단독**(사람 확인 입력 2종 필수) — 자동으로 돌지 않는다 |
| 검사 | `SOURCE_SHA`(`1bbbc214…`) checkout → PR #51 head 일치 확인 → analyze·test(**1508건 정확 일치**) → 운영 `.env` 생성·preflight → keystore 복원 → `appbundle --release` → jarsigner·bundletool·manifest·내장 env 검증 → artifact(3일) |
| 필요한 것 | GitHub **Environment `android-release-candidate`**(required reviewers + master 단독 배포 정책 권장) + 시크릿 7개: `SUPABASE_URL` · `SUPABASE_ANON_KEY` · `SENTRY_DSN` · `ANDROID_KEYSTORE_BASE64` · `ANDROID_KEYSTORE_PASSWORD` · `ANDROID_KEY_ALIAS` · `ANDROID_KEY_PASSWORD` |
| **원본 저장소 의존** | `EXPECTED_PR: "51"` — `gh api repos/${GITHUB_REPOSITORY}/pulls/51` 로 원본의 PR #51 head 를 대조한다. 새 저장소에는 PR 이 0개라 **1단계에서 실패**한다. `SOURCE_SHA` 커밋은 이 저장소에 존재하지만 `master` 조상이 아니다(유지한 3개 브랜치에만 있음). `EXPECTED_TEST_COUNT: "1508"` 도 현재 1,510(+골든)과 다르다 |
| 판정 | **비활성 상태로 둔다**(수동 전용이라 자연히 비활성). 재사용하려면 SOURCE_SHA·EXPECTED_PR·EXPECTED_TEST_COUNT 를 재고정하고 계약 테스트 ②를 함께 갱신해야 한다 — 배포 시점 작업 |

### iOS (Xcode Cloud)

`ios/ci_scripts/ci_post_clone.sh`(Flutter 3.44.6 핀) — GitHub Actions 가 아니라 **Xcode Cloud** 훅. 새 저장소를 Xcode Cloud 에 연결해야 동작한다(오너 콘솔 작업).

---

## 4. 구조 (§2-4)

### `lib/` — 228개 Dart 파일

| 디렉터리 | 파일 | 역할 |
|---|---|---|
| `lib/app/` | 5 | `router.dart`(GoRouter) · `app.dart`(MaterialApp.router + 역할별 테마 + VersionGateShell) · `home_shell.dart`(하단 5탭 IndexedStack) · `app_tabs.dart`(탭 상수·전역 탭 전환 채널) · `entry_guard.dart`(AccessState → redirect) |
| `lib/core/` | 44 | auth 3 · supabase 1 · config 1 · entitlement 5 · version_gate 10 · web_bridge 5 · deeplink 2 · scan 6 · ink 6 · refresh 1 · observability 1 · push 2 · commerce 1 |
| `lib/data/` | 1 | `mappings/subject_labels.dart`(과목 코드 → 한글) |
| `lib/design/` | 23 | 색·타이포·간격·모양 토큰, `theme.dart`, 공용 위젯 15종 |
| `lib/features/` | 144 | question_room 42 · community 32 · individual_question 19 · mypage 16 · mentors 15 · notifications 8 · scan_annotation 5 · auth 3 · dev 3 · onboarding 1 |
| `lib/shared/` | 10 | 상수 2 · 포맷터 · 에러 2 · 라벨 · 공용 위젯 4 |

### 라우트·탭·화면

- **선언적 라우트(GoRouter)**: `lib/app/router.dart:18-54` — **4개** `/splash` `/login` `/home` `/blocked` (+ 개발 빌드 전용 `/dev/gallery` `/dev/s3` 2개, `kDevToolsEnabled=!kReleaseMode`). redirect 는 `EntryGuard.redirect`(`entry_guard.dart:31-52`), `refreshListenable: AuthService.instance`.
- **하단 탭 5개**: `home_shell.dart:47-53` `QuestionRoomScreen · CommunityScreen · MentorsScreen · NotificationsScreen · IndividualQuestionTabScreen`. 인덱스 상수 `app_tabs.dart:10-14`. **마이페이지는 탭이 아니라** AppBar 우측 프로필 버튼 push(`home_shell.dart:120-133`, 가상 목적지 `AppTab.myPage=100`). 게스트 허용 탭 `{1,2}`(`entry_guard.dart:25`). 탭은 첫 방문 시 lazy build(N13).
- **화면 위젯**: `*_screen.dart` **36개** —
  auth 3(`splash`·`login`·`blocked`) · onboarding 1 · question_room 11(`question_room`·`question_list`·`chat`·`connection_notes`·`new_question`·`attachment_viewer`·`mentor_room_home`·`mentor/mentor_inbox`·`mentor/student_room_home`·`mentor/mentor_question_list`·`mentor/mentor_answer`) · community 6(`community`·`board/board_detail`·`board/board_write`·`shortform/shortform_detail`·`shortform/shortform_compose`·`blocks/blocked_users`) · individual_question 5(`individual_question_tab`·`student_iq_list`·`mentor_iq_list`·`iq_detail`·`iq_create`) · mentors 3(`mentors`·`mentor_detail`·`free_question_compose`) · mypage 3(`mypage`·`profile_edit`·`account_delete`) · notifications 1 · scan_annotation 1 · core/scan 1(`pdf_page_select`) · design 1(`empty_screen`).
- **명령형 내비게이션**: 라우트 4개 외 **모든 화면 이동은 `Navigator.push(MaterialPageRoute(...))`** — **44곳 / 26파일**. `context.go` 5곳(login 2 · onboarding 2 · mypage 1, 전부 `EntryGuard` 경로).

### 모델 인자 push (CC 조사 "46곳") — 실제 위치

`MaterialPageRoute` 44곳(CC 조사의 46은 다른 시점 수치로 본다). 인자 성격별:

| 성격 | 곳 | 위치(파일:줄) |
|---|---|---|
| **도메인 모델 객체 전달**(Room·QuestionThread·MentorPublic·MyProfile·post·image…) | 27 | question_room: `question_room_screen.dart:284`(Room→MentorRoomHome) · `ui/question_list_screen.dart:195,204,217`(Room→NewQuestion / Thread→Chat / Room→ConnectionNotes) · `ui/mentor_room_home_screen.dart:159,172` · `ui/mentor/mentor_inbox_screen.dart:215` · `ui/mentor/student_room_home_screen.dart:208,222` · `ui/mentor/mentor_question_list_screen.dart:224`(Thread→MentorAnswer) · `ui/chat_screen.dart:295,460` · `ui/mentor/mentor_answer_screen.dart:280,447` · `ui/attachment_viewer_screen.dart:54`(첨부→ScanAnnotation) — notifications `ui/notification_target_opener.dart:105,112,135,142,169,185,201`(알림→ 대상 화면 7종에 Room/Thread/item 전달) — mentors `mentors_screen.dart:350`(MentorPublic item→Detail) · `ui/widgets/free_question_entry_section.dart:140` — community `ui/board/board_list_view.dart:156` · `ui/activity/my_activity_view.dart:139` · `ui/board/board_detail_screen.dart:251` · `ui/shortform/shortform_feed_view.dart:182` — mypage `mypage_screen.dart:183`(MyProfile→ProfileEdit) — individual_question `ui/iq_create_screen.dart:233` · `ui/iq_detail_screen.dart:582,631,1495` — core `scan/widgets/scan_pick_expander.dart:49` |
| **ID(문자열)만 전달** | 5 | `ui/notification_target_opener.dart:157` · `ui/mentor_iq_list_screen.dart:137,154` · `ui/student_iq_list_screen.dart:105`(→`IqDetailScreen(questionId:)`) · `community_screen.dart:87`(레포 주입) |
| **인자 없음** | 12 | `app/home_shell.dart:126`(마이페이지) · `shared/widgets/withdrawal_pending_banner.dart:89` · `mypage/ui/sections/settings_section.dart:117,254` · `community/ui/shortform/shortform_feed_view.dart:165` 등 |

시사점(A-2): 딥링크·복원·뒤로가기 정합을 위해선 **ID 기반 라우트 + 화면 안 조회**로 바꿔야 하는 곳이 27곳이다. 목록·상세가 같은 객체를 공유하는 현재 방식은 화면 수십 개에서 유지하기 어렵다.

### 상태관리 방식

**패키지 없음**(provider/riverpod/bloc/get_it 미사용). `ChangeNotifier` + **static 싱글턴** + static `ValueNotifier` 버스 + 화면 State 가 `const XxxRepository()` 를 직접 생성(66곳).

| 싱글턴 | 위치 | 담는 것 | 직접 참조 |
|---|---|---|---|
| `AuthService.instance` | `lib/core/auth/auth_service.dart:32` | 세션·역할(`AppRole`)·표시명·계정상태(`AccountState`)·`AccessState`(진입 분기 단일 소스), auth 스트림 구독 | **32곳 / 19파일** (router·app·home_shell·question_room·mypage·mentors·community·notifications·IQ …) |
| `SupabaseInit` (static) | `lib/core/supabase/supabase_client.dart:5-26` | 초기화 플래그·`clientOrNull` | **56곳 / 40파일** — 레포지토리·화면 State 가 직접 `auth.currentUser?.id` 를 읽는다 |
| `DeletionNoticeController.instance` | `lib/core/auth/deletion_notice_controller.dart:25` | 탈퇴 잡 상태(배너·취소 가능) single-flight | home_shell 배너·mypage |
| `VersionGateController.instance` | `lib/core/version_gate/version_gate_controller.dart:46` | 버전 게이트 상태(checking/pass/forceUpdate/recommend/fetchFailed) | `main.dart:55` · `app.dart:41` |
| `DeepLinkService.instance` | `lib/core/deeplink/deep_link_service.dart:20` | 대기 딥링크·로그인 연동 | `main.dart:49` · auth_service |
| `NotificationBadgeController.instance` | `lib/features/notifications/data/notification_badge_controller.dart:26` | 알림 미읽음 수(`ValueNotifier<int?>`) | home_shell·notifications_screen |
| `TabNavigator.request` | `lib/app/app_tabs.dart:30` | 전역 탭 전환 요청 채널 | home_shell·딥링크 오프너 |
| `DataRefreshBus` (static ×4) | `lib/core/refresh/data_refresh_bus.dart:14,30,39,49` | wallet/subscription/notifications/questionRooms 재조회 세대 | 탭 화면들 |
| `AppRouter.router` | `lib/app/router.dart:18` | GoRouter 인스턴스 | app.dart |
| `AttachmentUrlResolver._shared` | `lib/features/question_room/data/attachments/attachment_url_resolver.dart:44-47` | 서명 URL 프로세스 전역 캐시 | chat·mentor_answer |
| `WebSessionHygiene._cleaner` | `lib/core/web_bridge/web_session_hygiene.dart:10` | WebView 쿠키 정리 콜백 | main·auth_service |
| `ContentPolicyGate.agreedThisSession` | `lib/features/community/ui/widgets/content_policy_gate.dart:23` | 세션 내 동의 플래그 | community |

주입 seam 은 **테스트 필요가 생긴 곳에만** 생성자 파라미터로 뚫려 있다(`readRepository`·`loaderOverride`·`realtimeFactory`·`safety`·`currentUserIdOverride` …). 없는 곳(예: `QuestionRoomScreen`/`_StudentRoomList`·`MentorInboxScreen`·`ConnectionNotesScreen` 의 쓰기 레포)은 렌더 자체가 백엔드에 묶인다 — 골든 §에서 확인.

---

## 5. 이관 자산 실재 확인 (§2-5)

기획서 §9-1 "유지" 항목의 실제 위치. **없거나 다른 형태면 그대로 적었다.**

| 자산 | 실재 | 위치(파일:줄) · 형태 |
|---|---|---|
| **계정 상태 fail-closed 판정** | **있음** | `lib/core/auth/account_status.dart` — `AccountStatusKind.fetchFailed`(`:36`, 재시도 가능 차단), `AccountState.allowsAppUse/isBlocked`(`:58-65`), 판정기 `AccountStatusReader.resolve`(`:226-`): users 행 조회 실패/행 없음 → fetchFailed, `account_deletion_write_blocked` RPC 실패 → fetchFailed, `account_deletion_status_self` 실패 → fetchFailed(잡 없음만 통과). 접근 상태 합성 `AuthService.computeAccess`(`auth_service.dart:88`). 포트 `AccountStatusGateway`(`:110`) + `PrefetchedUserRowGateway`(`:174`). 테스트 `test/auth/` 34건 |
| **RPC 응답 봉투 파서** | **다른 형태** — 공용 파서 없음 | `{ok: true, …}` 판정이 호출부마다 흩어져 있다(12+곳): `notifications_repository.dart:90 parseUnreadCountEnvelope`(contract_version 1 검사) · `board_post_create_gateway.dart:120 parseBoardPostCreateEnvelope` · `board_post_update_gateway.dart:113` · `individual_question_models.dart:564` · `individual_question_repository.dart:219` · `iq_attachment_upload_core.dart:176` · `question_room_read_repository.dart:154,175` · `free_question_entry.dart:177,209` · `community_write_repository.dart:110,208,246` · `account_deletion_repository.dart:237,330,347` · `profile_edit_repository.dart:115` · `account_status.dart:163`. **공통 규칙(ok 아니면 날조 금지·null 반환)은 일관되나 코드는 중복** — A-3/A-4 에서 단일 파서로 모을 후보 |
| **첨부 업로드 파이프라인** | **있음** | 질문방: `lib/features/question_room/data/attachments/attachment_upload.dart` — 포트 `AttachmentUploaderPort`(`:104`), 구현 `SupabaseAttachmentUploader`(`:123`; 경로 규약 `{roomId}/{threadId}/…`, RPC `qna_register_attachment`, 등록 실패 시 보상 삭제 `AttachmentRegistrationFailure :77`/충돌 `:98`), 백엔드 seam `QnaAttachmentBackend`(`:331`)/`SupabaseQnaAttachmentBackend`(`:356`). 개별질문: `individual_question/data/iq_attachment_upload_core.dart`(포트 typedef `:46-70`). 소스 선택·축소·PDF: `lib/core/scan/`(6파일) |
| **서명 URL 계약** | **있음, 4벌** | `question_room/data/attachments/attachment_url_resolver.dart` — `AttachmentUrlResolver`(`:29`, TTL 1h 메모리 캐시, 캐시 키에 사용자 id, 프로세스 전역 공유 `:44-47`), 백엔드 `SupabaseAttachmentUrlBackend`(`:80`); 신뢰 URL 검사 `trusted_attachment_url.dart:9 isTrustedAttachmentUri`. 같은 패턴이 `individual_question/data/iq_attachment_url_resolver.dart:30` · `community/data/community_post_image_url_resolver.dart:41` · `community/data/shortform_media_url_resolver.dart:83` 에 **각각 구현**돼 있다(중복 — 통합 후보) |
| **Realtime 계약** | **있음, 3벌** | `question_room/data/thread_realtime.dart` — 포트 `ThreadRealtimePort`(`:10`), 구현 `SupabaseThreadRealtime`(`:29`; 채널 `question_thread_$threadId`, `question_messages` INSERT · `question_threads` UPDATE · `question_attachments` INSERT, `:44-92`) — 콜백 미도착 시 재조회 폴백. `individual_question/data/iq_realtime.dart:39-115`(채널 `iq_$questionId`), `notifications/data/notifications_realtime.dart:47-67`(채널 `notifications_$userId`, INSERT) |
| **웹 브릿지** | **있음** | `lib/core/web_bridge/` — `web_bridge.dart` `WebBridge`(`:18`; https 만, `baseUrl` 미확정이면 `WebOpenResult.notConfigured` 안내 폴백 `:109`, launcher 주입), `web_bridge_config.dart`(`baseUrl = String.fromEnvironment('WEB_BASE_URL', default 운영 도메인)` `:16`; 경로 상수 `:24-46` — billing/payout/profile/terms/privacy/support/reviews/accountDelete/iqCreate), `web_bridge_actions.dart`(화면 헬퍼), `web_session_hygiene.dart`(WebView 쿠키 정리), `shortform_compose_bridge.dart`. 테스트 `test/web_bridge/` 4파일 |
| **앱 버전 게이트** | **있음** | `lib/core/version_gate/`(10파일) — `VersionGateController`(`version_gate_controller.dart:34`; 상태 `:16-26`), 포트 `VersionPolicyPort` → `SupabaseVersionPolicyPort`(`supabase_version_policy_port.dart:11`, RPC `get_mobile_app_version_policy(p_platform)` `:22`), 판정 `VersionGateDecision`(sealed, `version_gate_decision.dart:10`), 셸 `VersionGateShell`(`version_gate_shell.dart:13`; `app.dart:40` 에서 라우터 위에 얹음), 통과 캐시 `shared_prefs_gate_pass_cache.dart`, 스토어 URL `store_url_policy.dart`. `main.dart:55` 에서 runApp 전 시작(await 안 함). 테스트 `test/version_gate/` 7파일 |
| **매니페스트 잠금 테스트** | **있음** | `test/contracts/outbound_api_manifest_test.dart` — §2-2 ① 참조 |

---

## 6. 저장소 메타 확인

| 항목 | 값 | 처리 |
|---|---|---|
| Android `applicationId` / `namespace` | `com.ssambership.edu` (`android/app/build.gradle.kts:30,43`), minSdk 24 · targetSdk 36 | **불변** |
| iOS `PRODUCT_BUNDLE_IDENTIFIER` | `com.ssambership.app`(Runner ×3) · `.RunnerTests` ×3 | **불변** |
| `pubspec.yaml` `version` | `1.0.0+19` | **불변**(배포 시점에 정한다. 잠금 3곳: `test/app/build_version_test.dart`·`test/contracts/ios_release_config_contract_test.dart:360`·서명 워크플로+계약 ②) |
| `pubspec.yaml` `description` | 구 "읽기 중심·Commerce-Zero(앱 내 결제 없음)" | → **"쌤버십 모바일 앱 — 학습 멘토링. 캐시 충전을 제외한 전 기능 지원."** 으로 변경(테스트 잠금 없음) |
| Flutter 채널/버전 | `.metadata` stable `ad70ec46`; lock `dart >=3.12 <4`, `flutter >=3.44` | — |
