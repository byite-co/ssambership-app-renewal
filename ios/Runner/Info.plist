<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<!-- 개발용 로컬 Supabase(http://127.0.0.1 / LAN IP) 통신 허용.
	     NSAllowsLocalNetworking 은 '로컬 네트워크'에만 cleartext 를 허용하므로
	     출시(원격 https://*.supabase.co)에는 영향 없음 — ATS 전체 해제 아님. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<!-- App-F0: OS 푸시(APNs)는 출시 범위가 아니다. 푸시 entitlement·백그라운드
	     원격 알림 모드·푸시 SDK 설정 파일을 두지 않는다. 앱은 알림함(인앱)만 제공한다. -->
	<!-- image_picker(질문 첨부): 사진 선택 시 필요. 없으면 iOS 런타임 크래시. -->
	<key>NSPhotoLibraryUsageDescription</key>
	<string>질문에 첨부할 이미지를 선택하기 위해 사진 보관함 접근이 필요해요.</string>
	<!-- image_picker 촬영(S16): 카메라 사용 사유. 없으면 촬영 진입 시 크래시. -->
	<key>NSCameraUsageDescription</key>
	<string>문제를 촬영해 질문에 첨부하기 위해 카메라를 사용해요.</string>
	<!-- 숏폼 작성 WebView(WKWebView) 영상 촬영: iOS 는 영상 녹화 시 마이크 권한을
	     함께 요구한다. 이 키가 없으면 '촬영' 선택 순간 TCC 강제 종료(크래시) —
	     사진 촬영(카메라 키만 필요)·파일 업로드는 되고 영상 촬영만 죽는 증상의 원인. -->
	<key>NSMicrophoneUsageDescription</key>
	<string>숏폼 영상을 촬영할 때 소리를 함께 녹음하기 위해 마이크를 사용해요.</string>
	<!-- web_bridge: iOS canOpenURL 은 여기 선언된 스킴만 조회를 허용한다(미선언 스킴은 항상 false —
	     Apple UIApplication.canOpenURL 문서). web_bridge._defaultLauncher 가 canLaunchUrl 로
	     사전 확인하므로 선언이 없으면 약관·개인정보·지원·탈퇴 등 웹 링크 전 경로가 조용히 실패한다.
	     https 만 선언하는 이유: isAllowedUri 가 https 를 강제해 http 는 canLaunchUrl 에 도달할 수 없다
	     (test/contracts/ios_release_config_contract_test.dart 가 이 정합을 고정). -->
	<key>LSApplicationQueriesSchemes</key>
	<array>
		<string>https</string>
	</array>
	<!-- 수출규정(암호화): 앱의 암호화 사용은 ① OS 제공 TLS(HTTPS, Supabase) ② JWT 서명 검증
	     (supabase→gotrue→dart_jsonwebtoken→pointycastle — 인증·전자서명 용도) 뿐 = 면제 범주.
	     이 키가 없으면 업로드마다 수출규정 문답이 뜬다. 비면제 암호화 도입 시 재검토(런북 §7). -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>쌤버십</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>ssambership_app</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
